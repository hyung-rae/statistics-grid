import type { ContentSubtypes } from "@/types"

const useStatisticsList = (subType: ContentSubtypes) => {
  /**
   * sub type 에 따라서
   * api 옵션 목록들도 달라져야함
   *
   * feed
   * 오늘 방문자 수
   * 총 페이지 방문 수
   * 신규 방문자 수
   * 재방문자 수
   * 가장 많이 본 페이지
   */

  return {}
}

export default useStatisticsList
